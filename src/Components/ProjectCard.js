import React from 'react';
import ProjectCardEdit from './ProjectCardEdit'

function ProjectCard(props) {
    return (
        <div>
            Hello from ProjectCard!
            <ProjectCardEdit/>
        </div>
    );
}

export default ProjectCard;